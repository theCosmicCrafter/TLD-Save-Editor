<UserControl x:Class="The_Long_Dark_Save_Editor_2.Tabs.SkillsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:p="clr-namespace:The_Long_Dark_Save_Editor_2.Properties"
             xmlns:converters="clr-namespace:The_Long_Dark_Save_Editor_2.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <UserControl.Resources>
        <converters:SkillLevelConverter x:Key="SkillLevelConverter" />
        <converters:SkillProgressConverter x:Key="SkillProgressConverter" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 15">
                <Button Margin="0 0 10 0" Style="{StaticResource MaterialDesignFlatButtonNoRounding}" Click="MaxAllSkillsClicked" Content="Max All Skills" />
                <Button Style="{StaticResource MaterialDesignFlatButtonNoRounding}" Click="ResetAllSkillsClicked" Content="Reset All Skills" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.FireStartingSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Firestarting.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Firestarting.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Firestarting.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Firestarting.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.CarcassHarvestingSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.CarcassHarvesting.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.CarcassHarvesting.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.CarcassHarvesting.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.CarcassHarvesting.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.CookingSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Cooking.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Cooking.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Cooking.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Cooking.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.IceFishingSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.IceFishing.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.IceFishing.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.IceFishing.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.IceFishing.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.RifleSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Rifle.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Rifle.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Rifle.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Rifle.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.ArcherySP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Archery.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Archery.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Archery.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Archery.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.ClothingRepairSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.ClothingRepair.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.ClothingRepair.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.ClothingRepair.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.ClothingRepair.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.RevolverSkillSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Revolver.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Revolver.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Revolver.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Revolver.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                <Label VerticalAlignment="Center" Width="160" Content="{x:Static p:Resources.GunsmithingSkillSP}" />
                <Slider VerticalAlignment="Center" Width="200" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CurrentSave.Global.SkillsManager.Gunsmith.m_Points}" />
                <TextBox Text="{Binding CurrentSave.Global.SkillsManager.Gunsmith.m_Points}" Width="50" Margin="10 0" TextAlignment="Center" />
                <TextBlock VerticalAlignment="Center" Width="140" Text="{Binding CurrentSave.Global.SkillsManager.Gunsmith.m_Points, Converter={StaticResource SkillLevelConverter}}" />
                <ProgressBar VerticalAlignment="Center" Width="120" Maximum="1" Value="{Binding CurrentSave.Global.SkillsManager.Gunsmith.m_Points, Converter={StaticResource SkillProgressConverter}}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
