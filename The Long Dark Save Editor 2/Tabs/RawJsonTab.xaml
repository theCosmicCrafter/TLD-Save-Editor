<UserControl x:Class="The_Long_Dark_Save_Editor_2.Tabs.RawJsonTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:p="clr-namespace:The_Long_Dark_Save_Editor_2.Properties"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10 5">
            <ToggleButton Name="tgEnableEdit" Style="{StaticResource MaterialDesignSwitchToggleButton}" Content="Enable Editing" />
            <Button Name="btnApply" Margin="20 0 0 0" Style="{StaticResource MaterialDesignFlatButtonNoRounding}"
                    Click="ApplyClicked" Content="Apply Changes" IsEnabled="False" />
            <Button Margin="10 0 0 0" Style="{StaticResource MaterialDesignFlatButtonNoRounding}"
                    Click="ReloadClicked" Content="Reload JSON" />
        </StackPanel>
        <Rectangle DockPanel.Dock="Top" Fill="#A6B8C3" Height="1" />
        <TextBox Name="txtJson" DockPanel.Dock="Bottom" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" IsReadOnly="True"
                 Margin="10" />
    </DockPanel>
</UserControl>
